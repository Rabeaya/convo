'use client';

/**
 * DockedChat
 *
 * Placeholder component for the docked chat window.
 * The layout expects this component to exist, but the chat feature itself
 * is not yet implemented in this repo. Keeping it as a no-op preserves
 * current functionality while preventing build failures.
 */
export default function DockedChat() {
  return null;
}


